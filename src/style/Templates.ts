import { css } from "styled-components";

export const centeredAbsolute = css`
	position: absolute;
	top: 50%;
	left: 50%;
	transform: translate(-50%, -50%);
`;

export const centeredFlexy = css`
	place-items: center;
`;
