import styled from "styled-components";

const Picker = styled.div`
	width: 30px;
	height: 30px;
	border-radius: var(--border-radius-circle);
`;

const ChatMoreThemePicker = () => {
	return <div>ChatMoreThemePicker</div>;
};

export default ChatMoreThemePicker;
